<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.one.one_to_one.OneToOneMapper">
    <resultMap id="one2one" type="com.one.bean.Card">
        <id column="id" property="id"/>
        <result column="number" property="number"/>
        <association property="person" javaType="com.one.bean.Person">
            <id column="pid" property="id"/>
            <result column="name" property="name"/>
            <result column="age" property="age"/>
        </association>
    </resultMap>


    <select id="selectAll" resultMap="one2one">
        select c.id,number, p.id pid,p.name ,p.age from card c, person p where c.pid = p.id
    </select>
</mapper>